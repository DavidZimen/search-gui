'use strict';

(function (window) {
    window['env'] = window['env'] || {};

    // Tariff Engine API URL
    window['env']['searchServiceUrl'] = undefined;
    window['env']['opaUrl'] = undefined;
}(this));
