apiVersion: v1
kind: Service
metadata:
  labels:
    app: search-gui
  name: search-gui
spec:
  type: NodePort
  ports:
    - nodePort: {{ .Values.service.nodePort }}
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app: search-gui
